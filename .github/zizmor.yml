# zizmor configuration (auto-discovered from .github/zizmor.yml)
# Docs: https://docs.zizmor.sh/configuration/
#
# skills-installer has no unpinned workflows: every third-party action is
# SHA-pinned with a trailing comment that documents the human-readable tag,
# and Dependabot keeps those SHAs fresh.

rules: {}
